---
import Icon from './Icon.astro';
---

<button type="button" class="search-trigger" data-search-open aria-haspopup="dialog">
	<Icon name="search" size={15} />
	<span class="search-trigger-label">Hledat</span>
	<kbd class="search-trigger-key">/</kbd>
</button>

<dialog class="search-dialog" data-search-dialog aria-label="Hledat" aria-modal="true">
	<div class="search-dialog-inner">
		<div class="search-field">
			<Icon name="search" size={16} />
			<input
				type="text"
				class="search-input"
				data-search-input
				role="combobox"
				aria-expanded="true"
				aria-controls="search-results"
				aria-autocomplete="list"
				placeholder="Hledat téma nebo slovo…"
				autocomplete="off"
				spellcheck="false"
				aria-label="Hledat"
			/>
			<button type="button" class="search-close" data-search-close aria-label="Zavřít hledání">
				<Icon name="close" size={15} />
			</button>
		</div>
		<p class="search-status" data-search-hint>Piš název tématu, nadpisu nebo slovo z úvodu stránky — diakritika není potřeba.</p>
		<p class="search-status" data-search-loading hidden>Načítám rejstřík…</p>
		<p class="search-status" data-search-empty hidden>Nic jsme nenašli. Zkus jiné slovo.</p>
		<ul class="search-results" id="search-results" data-search-results role="listbox" aria-label="Výsledky hledání"></ul>
	</div>
</dialog>

<script>
	import { search, type Hit, type SearchItem } from '../lib/search-match';

	function initSearch() {
		const openBtn = document.querySelector<HTMLButtonElement>('[data-search-open]');
		const dialog = document.querySelector<HTMLDialogElement>('[data-search-dialog]');
		const closeBtn = document.querySelector<HTMLButtonElement>('[data-search-close]');
		const input = document.querySelector<HTMLInputElement>('[data-search-input]');
		const results = document.querySelector<HTMLUListElement>('[data-search-results]');
		const hint = document.querySelector<HTMLElement>('[data-search-hint]');
		const loading = document.querySelector<HTMLElement>('[data-search-loading]');
		const empty = document.querySelector<HTMLElement>('[data-search-empty]');
		if (!openBtn || !dialog || !closeBtn || !input || !results || !hint || !loading || !empty) return;

		let items: SearchItem[] | null = null;
		let indexPromise: Promise<void> | null = null;
		let activeIndex = -1;
		let lastFocused: HTMLElement | null = null;

		function ensureIndexLoaded() {
			if (!indexPromise) {
				indexPromise = fetch('/search.json')
					.then((r) => r.json())
					.then((data: SearchItem[]) => {
						items = data;
						renderResults(input!.value);
					})
					.catch(() => {
						// offline or the request failed — leave items null so
						// renderResults keeps showing the loading line rather
						// than silently pretending there are zero results
						indexPromise = null;
					});
			}
			return indexPromise;
		}

		function setActive(index: number) {
			const rows = results!.querySelectorAll<HTMLLIElement>('.search-result');
			if (activeIndex >= 0 && rows[activeIndex]) rows[activeIndex].classList.remove('is-active');
			activeIndex = index;
			const row = rows[activeIndex];
			if (row) {
				row.classList.add('is-active');
				input!.setAttribute('aria-activedescendant', row.id);
				row.scrollIntoView({ block: 'nearest' });
			} else {
				input!.removeAttribute('aria-activedescendant');
			}
		}

		function moveSelection(delta: number) {
			const count = results!.querySelectorAll('.search-result').length;
			if (!count) return;
			setActive((activeIndex + delta + count) % count);
		}

		function activateSelection() {
			const rows = results!.querySelectorAll<HTMLLIElement>('.search-result');
			const row = rows[activeIndex] ?? rows[0];
			const a = row?.querySelector('a');
			if (a) location.href = a.href;
		}

		function renderResults(query: string) {
			results!.innerHTML = '';
			activeIndex = -1;
			input!.removeAttribute('aria-activedescendant');

			if (!items) {
				hint!.hidden = true;
				empty!.hidden = true;
				loading!.hidden = false;
				return;
			}
			loading!.hidden = true;

			const q = query.trim();
			if (!q) {
				empty!.hidden = true;
				hint!.hidden = false;
				return;
			}
			hint!.hidden = true;

			const matches: Hit[] = search(items, q);
			if (matches.length === 0) {
				empty!.hidden = false;
				return;
			}
			empty!.hidden = true;

			matches.forEach((hit, i) => {
				const { item } = hit;
				const li = document.createElement('li');
				li.id = `search-result-${i}`;
				li.className = 'search-result';
				li.setAttribute('role', 'option');

				const a = document.createElement('a');
				a.href = hit.heading && hit.heading[1] ? `${item.url}#${hit.heading[1]}` : item.url;

				const title = document.createElement('span');
				title.className = 'search-result-title';
				title.textContent = hit.heading ? hit.heading[0] : item.title;

				const meta = document.createElement('span');
				meta.className = 'search-result-meta';
				meta.textContent = (hit.heading ? `${item.title} · ` : '') + [item.subject, item.group].filter(Boolean).join(' › ');

				a.append(title, meta);
				const note = hit.term ? `Pojem: ${hit.term}` : hit.snippet;
				if (note) {
					const snippet = document.createElement('span');
					snippet.className = 'search-result-snippet';
					snippet.textContent = note;
					a.append(snippet);
				}
				li.append(a);
				li.addEventListener('mouseenter', () => setActive(i));
				results!.appendChild(li);
			});
			setActive(0);
		}

		function openDialog() {
			lastFocused = document.activeElement as HTMLElement | null;
			if (!dialog!.open) dialog!.showModal();
			input!.value = '';
			ensureIndexLoaded();
			renderResults('');
			requestAnimationFrame(() => input!.focus());
		}

		openBtn.addEventListener('click', openDialog);
		closeBtn.addEventListener('click', () => dialog!.close());

		// Standard native-<dialog> "light dismiss": a click that lands on
		// the ::backdrop bubbles a click event whose target is the <dialog>
		// element itself (nothing else covers that area), so this can't
		// misfire for a click on the actual dialog content.
		dialog.addEventListener('click', (e) => {
			if (e.target === dialog) dialog!.close();
		});

		dialog.addEventListener('close', () => {
			lastFocused?.focus();
		});

		input.addEventListener('input', () => renderResults(input!.value));
		input.addEventListener('keydown', (e) => {
			if (e.key === 'ArrowDown') {
				e.preventDefault();
				moveSelection(1);
			} else if (e.key === 'ArrowUp') {
				e.preventDefault();
				moveSelection(-1);
			} else if (e.key === 'Enter') {
				e.preventDefault();
				activateSelection();
			}
		});

		// Site-wide "/" shortcut, guarded against firing while the visitor
		// is already typing somewhere else — including the maths
		// scratchpad's <math-field> custom element, which behaves like a
		// text input but isn't one.
		document.addEventListener('keydown', (e) => {
			if (e.key !== '/' || e.metaKey || e.ctrlKey || e.altKey || dialog!.open) return;
			const target = e.target as HTMLElement | null;
			const tag = target?.tagName;
			if (tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT' || tag === 'MATH-FIELD' || target?.isContentEditable) {
				return;
			}
			e.preventDefault();
			openDialog();
		});
	}

	initSearch();
</script>

<style>
	.search-trigger {
		flex: 0 0 auto;
		display: flex;
		align-items: center;
		gap: 0.45rem;
		margin-left: 1.25rem;
		padding: 0.4rem 0.65rem;
		border: 1px solid var(--border);
		border-radius: 2px;
		background: none;
		color: var(--muted);
		font-family: var(--sans);
		font-size: var(--t-sm);
		cursor: pointer;
		transition: border-color 0.15s ease, color 0.15s ease;
	}

	.search-trigger:hover,
	.search-trigger:focus-visible {
		color: var(--fg);
		border-color: var(--border-strong);
	}

	.search-trigger-key {
		font-family: var(--sans);
		font-size: 0.72rem;
		line-height: 1.3;
		padding: 0 0.35rem;
		border: 1px solid var(--border);
		border-radius: 2px;
		color: var(--muted);
	}

	@media (max-width: 640px) {
		/* icon only — there's no physical "/" key affordance to advertise
		   on a touch device, and the lone glyph without "Hledat" next to
		   it read as an orphaned fragment rather than a shortcut hint */
		.search-trigger-label,
		.search-trigger-key {
			display: none;
		}
		.search-trigger {
			gap: 0;
			margin-left: 0.75rem;
			padding: 0.4rem;
		}
	}

	/* ── Dialog shell ──────────────────────────────────────────────────
	   Positioned toward the top rather than dead-center (a command
	   palette, not a modal alert) — margin overrides the UA stylesheet's
	   auto-centering on the vertical axis only. */
	.search-dialog {
		width: min(34rem, calc(100vw - 2rem));
		margin: 12vh auto auto;
		padding: 0;
		border: 1px solid var(--border-strong);
		border-radius: 0;
		background: var(--surface-raised);
		color: var(--fg);
		/* opening is the one moment here that answers a direct action (a
		   click or the "/" shortcut), so it earns a small settle-in rather
		   than the hard cut every other state change on this site gets —
		   `allow-discrete` is what lets the *closing* half play too, since
		   without it the dialog would leave the top layer (and stop being
		   painted) the instant .close() runs, before the fade could show */
		opacity: 1;
		transform: translateY(0);
		transition: opacity 0.18s ease, transform 0.18s ease, overlay 0.18s allow-discrete, display 0.18s allow-discrete;
	}

	.search-dialog::backdrop {
		background: rgba(10, 8, 6, 0.72);
		opacity: 1;
		transition: opacity 0.18s ease, overlay 0.18s allow-discrete, display 0.18s allow-discrete;
	}

	/* starting-style = the state it animates *from* on open; browsers
	   without support just skip straight to the resting state above,
	   which is a perfectly fine instant open, not a broken one */
	@starting-style {
		.search-dialog[open] {
			opacity: 0;
			transform: translateY(-0.5rem);
		}

		.search-dialog[open]::backdrop {
			opacity: 0;
		}
	}

	.search-dialog:not([open]) {
		opacity: 0;
		transform: translateY(-0.5rem);
	}

	.search-dialog-inner {
		display: flex;
		flex-direction: column;
		max-height: 70vh;
	}

	.search-field {
		flex: 0 0 auto;
		display: flex;
		align-items: center;
		gap: 0.6rem;
		padding: 0.9rem 1rem;
		border-bottom: 1px solid var(--border);
		color: var(--muted);
	}

	.search-input {
		flex: 1 1 auto;
		min-width: 0;
		background: none;
		border: none;
		color: var(--fg);
		font-family: var(--sans);
		font-size: 1.02rem;
	}

	.search-input:focus {
		outline: none;
	}

	.search-input::placeholder {
		color: var(--muted);
	}

	.search-close {
		flex: 0 0 auto;
		display: flex;
		align-items: center;
		justify-content: center;
		width: 1.6rem;
		height: 1.6rem;
		padding: 0;
		border: none;
		border-radius: 2px;
		background: none;
		color: var(--muted);
		cursor: pointer;
	}

	.search-close:hover,
	.search-close:focus-visible {
		color: var(--fg);
		background: var(--surface);
	}

	.search-status {
		flex: 0 0 auto;
		margin: 0;
		padding: 1.1rem 1rem;
		font-family: var(--sans);
		font-size: var(--t-sm);
		color: var(--muted);
	}

	/* flex: 1 1 auto + min-height: 0 is the actual pairing that makes this
	   scroll — flex-grow alone still leaves a flex child sized to its
	   content, so up to 40 results could push the dialog past the
	   viewport instead of scrolling inside it. */
	.search-results {
		flex: 1 1 auto;
		min-height: 0;
		list-style: none;
		margin: 0;
		padding: 0.4rem;
		overflow-y: auto;
		scrollbar-width: thin;
		scrollbar-color: var(--border-strong) transparent;
	}
</style>
